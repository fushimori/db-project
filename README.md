# DB Project - Агрегатор Азиатского Медиа-Контента


Полнофункциональная веб-платформа для агрегации и управления азиатским медиа-контентом (аниме, манга, фильмы, книги, сериалы).

## 🎯 Описание

DB Project — это современное веб-приложение, позволяющее пользователям:
- 📚 Просматривать каталог азиатского медиа-контента
- ⭐ Создавать персональные списки просмотра
- 🔍 Искать и фильтровать медиа по различным критериям
- 👥 Взаимодействовать с другими пользователями
- 📊 Оценивать и рецензировать контент

## 🛠 Технологический стек

### Backend
- **Python 3.9+** - основной язык программирования
- **FastAPI** - современный веб-фреймворк
- **AsyncPG** - асинхронный драйвер PostgreSQL
- **PyJWT** - работа с JSON Web Tokens
- **Pydantic** - валидация данных
- **Passlib** - хеширование паролей

### Frontend
- **React 19.0** - UI библиотека
- **React Router 7** - клиентская маршрутизация
- **Axios** - HTTP клиент
- **JWT-Decode** - декодирование токенов

### База данных и инфраструктура
- **PostgreSQL 13** - реляционная СУБД
- **Docker & Docker Compose** - контейнеризация
- **Migrations** - версионирование схемы БД

## 🚀 Быстрый старт

### Предварительные требования
- Docker и Docker Compose
- Git

### Клонирование и запуск

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd db-project

# Запустите все сервисы
docker-compose up --build

# Приложение будет доступно по адресам:
# Frontend: http://localhost:3000
# Backend API: http://localhost:8000
# PostgreSQL: localhost:5432
```

### Первый запуск

1. **Дождитесь инициализации всех контейнеров**
2. **Админ-аккаунт создается автоматически:**
   - Username: `admin`
   - Email: `admin@admin.com`
   - Password: `admin`

3. **Доступ к API документации:**
   - Swagger UI: http://localhost:8000/docs
   - ReDoc: http://localhost:8000/redoc

## 📁 Структура проекта

```
db-project/
├── backend/                    # FastAPI приложение
│   ├── app/                   # Основное приложение
│   │   ├── main.py            # Точка входа
│   │   ├── auth.py            # Аутентификация
│   │   ├── media.py           # Медиа-логика
│   │   ├── anime.py           # API аниме
│   │   ├── manga.py           # API манги
│   │   ├── movie.py           # API фильмов
│   │   ├── book.py            # API книг
│   │   ├── series.py          # API сериалов
│   │   ├── person.py          # API персон
│   │   ├── user.py            # Управление пользователями
│   │   └── profile.py         # Профили пользователей
│   └── db/                    # Утилиты БД
├── frontend/                  # React приложение
│   └── my-app/
│       ├── src/
│       │   ├── components/    # UI компоненты
│       │   ├── api/          # API клиенты
│       │   └── context/      # React Context
├── db/                       # Конфигурация БД
│   ├── migrations/           # SQL миграции
│   └── Dockerfile           # PostgreSQL контейнер
├── static/                  # Статические файлы
│   ├── anime/              # Постеры аниме
│   ├── manga/              # Обложки манги
│   └── user/               # Аватары
└── docker-compose.yml      # Конфигурация сервисов
```

## 🔌 API Эндпоинты

### Аутентификация
- `POST /user/register` - Регистрация пользователя
- `POST /user/token` - Авторизация (получение JWT токена)

### Медиа контент
- `GET /anime/` - Список аниме
- `GET /anime/{id}/` - Детали аниме
- `GET /anime/search` - Поиск аниме
- `POST /anime/{id}/add-to-list` - Добавить в список просмотра
- `POST /anime/{id}/remove-from-list` - Удалить из списка

*Аналогичные эндпоинты для manga, movie, book, series*

### Административные функции
- `POST /anime/upload-csv` - Импорт CSV данных (только админ)
- `DELETE /anime/delete-media/{id}` - Удаление медиа (только админ)

## 🔧 Разработка

### Локальная разработка Backend
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### Локальная разработка Frontend
```bash
cd frontend/my-app
npm install
npm start
```

### Миграции базы данных
```bash
# Пересоздать контейнер БД для применения миграций
docker-compose restart db
```

## 📊 Модель данных

### Основные сущности
- **MediaContent** - центральная таблица всех медиа
- **Users** - пользователи с ролевой моделью
- **UserMediaList** - персональные списки просмотра
- **Ratings** - пользовательские оценки
- **Reviews** - отзывы пользователей
- **Persons** - информация о создателях/актерах

### Поддерживаемые типы медиа
- **anime** - Аниме
- **manga** - Манга
- **movie** - Фильмы
- **book** - Книги
- **series** - Сериалы

## 🔒 Безопасность

- JWT токены для аутентификации
- Хеширование паролей с bcrypt
- CORS настройки для безопасности
- Ролевая модель (admin, user)
- Валидация входных данных

## 📝 Использование

1. **Регистрация/Вход**: Создайте аккаунт или войдите как admin
2. **Просмотр каталога**: Перейдите в разделы Anime, Manga, Movies и т.д.
3. **Поиск**: Используйте строку поиска для фильтрации контента
4. **Списки просмотра**: Добавляйте медиа в свои списки со статусами (watching, completed, planned, on_hold, dropped)
5. **Администрирование**: Админы могут загружать CSV данные и удалять медиа

